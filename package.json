{
  "name": "crm-anselmotech",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "test": "npm --prefix frontend run lint && npm --prefix frontend run test && cd backend && python3 -m pytest tests -q",
    "build": "npm --prefix frontend run build"
  }
}
